version: '3.9'

services:
  mcp-server:
    build: .
    container_name: mcp_server
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - .:/app
    command: >
      bash -c "uvicorn services.mcp_server.router_agent:app --host 0.0.0.0 --port 8001"